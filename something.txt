from evaluators.registry import EvaluatorRegistry
from evaluators.dummy.always_one import AlwaysOneEvaluator


def build_registry() -> EvaluatorRegistry:
    registry = EvaluatorRegistry()

    # Dummy evaluator (for now)
    registry.register(AlwaysOneEvaluator)

    return registry
